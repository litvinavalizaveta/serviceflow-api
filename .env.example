# Optional local overrides for docker compose.
# Values here are development-only and are not production secrets.

POSTGRES_DB=serviceflow
POSTGRES_USER=serviceflow
POSTGRES_PASSWORD=serviceflow

SERVICEFLOW_HTTP_PORT=8080
SEED_DATA_RUN_ON_STARTUP=true
